<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <record id="action_print_model_account_move" model="ir.actions.server">
            <field name="name">Đề nghị thanh toán</field>
            <field name="model_id" ref="account_saving.model_account_move"/>
            <field name="binding_model_id" ref="account_saving.model_account_move"/>
            <field name="x_print_menu" eval="True"/>
            <field name="state">code</field>
            <field name="code">
                if records:
                attachment_id = records.get_job_description()
                action = {
                'type': 'ir.actions.act_url',
                'url': '/web/content/' + str(attachment_id.id) + '?download=true',
                'url': "web/content/?model=ir.attachment&amp;id=" + str(attachment_id.id) + "&amp;filename_field=name&amp;field=datas&amp;download=true&amp;name="
                + attachment_id.name,
                'target': 'new',
                }
            </field>
        </record>
    </data>

</odoo>