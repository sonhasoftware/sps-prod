<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="action_print_training_form" model="ir.actions.server">
            <field name="name">Training Form (.docx)</field>
            <field name="model_id" ref="hr_training.model_hr_training_result"/>
            <field name="binding_model_id" ref="hr_training.model_hr_training_result"/>
            <field name="x_print_menu" eval="True"/>
            <field name="state">code</field>
            <field name="code">
            if records:
                attachment_id = records.get_docx_data()
                action = {
                    'type': 'ir.actions.act_url',
                    'url': '/web/content/' + str(attachment_id.id) + '?download=true',
                    'url': "web/content/?model=ir.attachment&amp;id=" + str(attachment_id.id) + "&amp;filename_field=name&amp;field=datas&amp;download=true&amp;name=" + attachment_id.name,
                    'target': 'new',
                }
            </field>
        </record>

  </data>
</odoo>
